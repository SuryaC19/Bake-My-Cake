export type Product = {
    id ?: number
    name ?: string
    description ?: string
    price ?: number
    category ?: string
    imageUrl ?: string
    ratings ?: number
    unit ? : number
}